<?php
/**
 * config file
 */
return array(
	'feed_url' => '/feed', // the url that the feed should exist at
	'post_key' => 'datetime', // the unique meta attribute that only posts have
  'pages_order' => 'meta.date:desc meta.title:asc',
	);
